1. Get a more wholistic example setup
2. Enable a user to rejoin a codespace after it's been created
3. Create repo as needed rather than requiring it to be created in advance
4. After a successful test run -> VSCode api call to mark the question completed
5. Remove the codespace after question completion
